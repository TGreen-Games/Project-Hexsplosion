

public static class Enums {

    public enum PlayerStage { Expand, Neutral, Fill};

    public enum AiStage { Neutral, Attack, Defence };

	public enum Scenes {Start, Game, ScoreScreen, Tutorial}
    
	public enum TutorialStage{Intro, Expanding, Filling, CapturedTiles, GettingStunned,OtherPlayers,Stun,SweetRevenge,Collison, StatusBar,EndTutorial}
    
}
